
# 📘 全栈开发 Day 1：Node.js 后端思维觉醒

### 1. 核心概念：环境变了 (Runtime) 🌍

* **前端 (浏览器):** 有 `window` 和 `document` (DOM)，主要负责画页面。
* **后端 (Node.js):** **没有** `window` 和 DOM。
* 顶层对象是 `global`。
* 拥有 **文件系统 (fs)** 和 **网络 (http)** 的操作权限。
* *比喻：前端是餐厅的前厅（装修、服务），后端是后厨（处理食材、逻辑）。*



### 2. 模块化：CommonJS 规范 🧩

Node.js 中文件默认是隔离的，必须“显式”导入导出。

* **导出:** `module.exports = 变量/函数;`
* **导入:** `const tools = require('./path/to/file');`
* **特点:** `require` 是**同步**执行的。

### 3. 创建服务器：HTTP 模块 📡

不依赖任何第三方库，原生 Node.js 就能起服务。

```javascript
const http = require('http');

const server = http.createServer((req, res) => {
    // req (Request): 📥 浏览器发来的点菜单 (url, method等)
    // res (Response): 📤 也就是你要端给浏览器的菜
});

```

### 4. 路由控制 (Routing) 🚦

后端必须手动判断用户想去哪里。

* 原理：通过 `if (req.url === '/home')` 来区分不同页面。
* 如果不处理，所有地址都会返回一样的内容。
* 遇到未知的地址，标准的做法是返回 **404 状态码**。

### 5. 文件系统与异步 (fs) 📂

* **模块:** `require('fs')`
* **读取:** `fs.readFile('./index.html', (err, data) => { ... })`
* **重点:** Node.js 是**非阻塞 I/O**。读取文件的动作是**异步**的，所以发送响应的代码 `res.end(data)` 必须写在**回调函数内部**，否则拿不到数据。

### 6. 静态资源与 Content-Type 🎨

* **痛点:** 浏览器只看 URL 不知道文件是什么格式。
* **解决:** 必须在响应头 (Header) 中明确告诉浏览器。
* HTML: `res.setHeader('Content-Type', 'text/html')`
* CSS: `res.setHeader('Content-Type', 'text/css')` (没有这个，CSS 就无法生效！)
* 纯文本: `text/plain`
